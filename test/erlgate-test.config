%%%===================================================================
%%% ErlGate - TEST CONFIGURATION FILE
%%%===================================================================

[
    
    {erlgate, [
        
        %% Incoming messages to node
        {channels_in, [
            %% {ListenerPort, DispatcherModule, DispatcherOptions, TransportSpec}
            {8950, erlgate_test_dispatcher, [], tcp},
            {8951, erlgate_test_dispatcher_2, [], tcp}
        ]},
        
        %% Outgoing messages to nodes
        {channels_out, [
            %% {Ref, Host, Port, ConnectionNum, TransportSpec}
            {node_1, "localhost", 8900, 1, tcp},
            {node_2, "localhost", 8901, 1, tcp},
            {node_with_option, "localhost", 8902, 1, tcp},
            {node_error, "localhost", 8999, 1, tcp}
        ]}
    
    ]}

].
