%%%===================================================================
%%% ErlGate - TEST CONFIGURATION FILE
%%%===================================================================

[
    
    {erlgate, [
    
        %% Incoming messages to node
        {channels_in, [
            %% {ListenerPort, DispatcherModule}
            {8991, erlgate_test_dispatcher},
            {8992, erlgate_test_dispatcher_2}
        ]},
        
        %% Outgoing messages to nodes
        {channels_out, [
            %% {Ref, Host, RemotePort, ConnectionNum}
            {node_1, "localhost", 8989, 1},
            {node_2, "localhost", 8990, 1}
        ]}
    
    ]}

].
