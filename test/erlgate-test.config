%%%===================================================================
%%% ErlGate - TEST CONFIGURATION FILE
%%%===================================================================

[

%% ErlGate config
    {erlgate, [
    
        %% dispatcher
        {dispatcher_module, erlgate_test_dispatcher},
        
        %% ----- Listeners
        %% port
        {port, 8989},
    
        %% ----- Connectors
        {remote_clusters, [
            %% {cluster_id, servers}
            {cluster_2, [
                %% {Host, Port, # of connections}
                {"localhost", 8990, 1}
            ]}
        ]}

    ]}

].
