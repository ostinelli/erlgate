%%%===================================================================
%%% ErlGate - Configuration file
%%%===================================================================

[
    
    {erlgate, [
        
        %% Incoming messages to node
        {channels_in, [
            %% {ListenerPort, DispatcherModule, Options}
            {8989, erlgate_test_dispatcher, []},
            {8990, erlgate_test2_dispatcher, []}
        ]},
        
        %% Outgoing messages to nodes
        {channels_out, [
            %% {ChannelRef, Host, RemotePort, ConnectionNum}
            {node_1, "localhost", 8991, 1},
            {node_2, "localhost", 8992, 1}
        ]}
    
    ]}

].
