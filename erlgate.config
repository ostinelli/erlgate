%%%===================================================================
%%% ErlGate - Configuration file
%%%===================================================================

[

    {erlgate, [
    
        %% dispatcher
        {dispatcher_module, erlgate_test_dispatcher},
        
        %% ----- Listeners
        %% port
        {port, 8989},

        %% ----- Connectors
        {remote_clusters, [
            %% {cluster_id, servers}
            {ap_cluster, [
                %% {Host, Port, # of connections}
                {"localhost", 8990, 1}
            ]}
        ]}
    ]}

].
